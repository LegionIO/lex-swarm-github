inherit_gem:
  rubocop-legion: config/lex.yml

# Private helper methods return scalars/arrays — not public runner return values
Legion/Extension/RunnerReturnHash:
  Enabled: false

# Runner modules used as mixins — extend self not applicable
Legion/Extension/RunnerIncludeHelpers:
  Enabled: false
